Ghost v2.5D Private Vault Fix

- Adds a fallback + Photo button beside + Album on Photo Vault home so it cannot disappear.
- Makes Private a permanent protected album in Photo Vault.
- Adds Private Vault password gate for Photo Private.
- Attempts to gate Video Private using existing video album open function.
- Default Private password is 1234 and can be changed from the unlock popup.
- Keeps clean package assets only.

Syntax check:
JavaScript syntax check passed.

Missing:
None
