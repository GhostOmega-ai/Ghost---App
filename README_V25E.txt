Ghost v2.5E Private Folder Security

- Photo Vault main page now uses a hard visible + Picture button above + Album.
- Private folders are permanent/protected.
- Photo Private and Video Private ask for the Private password every time they are opened.
- Private folder cards always show private-vault-cover.png instead of album contents.
- Initial app setup now asks for a separate Private folder password after disguise setup.
- Existing installs default to Private password 1234 until changed.
- Clean package only.

Syntax check:
JavaScript syntax check passed.

Missing:
None
