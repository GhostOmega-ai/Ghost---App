Ghost v2.6 Clean Index Foundation

Purpose:
- Start a cleaner index foundation without adding the secondary passcode feature yet.

Fixes included:
- Hard restores + Photo and + Album on the main Photo Vault page.
- Photo albums now use Library, Favourites, Family, Private + custom albums.
- Removes old Default/Work/Documents photo album behaviour and migrates them to Private.
- Library and Favourites are protected from rename/delete.
- Video Vault migrates Other to Private.
- Bottom Home/Hide bar is fixed and permanent while content scrolls behind it.
- Adds no-cache service worker to reduce stale PWA/GitHub display.

Next recommended phase:
- v2.7 Private Album Security: optional second passcode + locked cover option.

Syntax check:
JavaScript syntax check passed.
