Ghost v2.6 Clean Photo Foundation

Built from index (25).html.

Cleaned:
- Removed the late v2.5G force override.
- Removed the older duplicate drawAlbumHome block.
- Kept one Photo Vault album-home renderer with + Picture and + Album.
- Forced Private photo album cover to private-vault-cover.png.
- Kept existing storage keys and IndexedDB names intact.

Counts after cleanup:
{'async function drawAlbumHome': 1, 'drawAlbumHome = async function': 0, 'function openAlbum(': 1, 'openAlbum = function': 0, '+ Picture': 1}

Syntax check:
JavaScript syntax check passed.

Missing files:
None
